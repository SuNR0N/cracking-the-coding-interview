/** Rotates the given input matrix by 90 degree */
export function rotateMatrix(image: number[][]): number[][] {
    const rotationMatrix90: number[][] = [
        [0, -1],
        [1, 0],
    ];
    const rotationMatrix180: number[][] = [
        [-1, 0],
        [0, -1],
    ];

    return [];
}
